<h1>New Books</h1>
<%= form_with model: @post_image, local:true do |f| %>
  <h4>Title</h4>
  <%= f.text_area :title %>
  <h4>Body</h4>
  <%= f.text_area :body %>
  <%= f.submit '投稿' %>
<% end %>

<%= paginate @books %>
